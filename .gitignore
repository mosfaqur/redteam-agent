# ============================================
# Claude Code / IDE (dev workspace)
# ============================================
/.claude/
/.codex/
/.worktrees/
/CLAUDE.md
/AGENTS.md
/LOCAL_REVIEW_WORKFLOW.md
/opencode.json
/.hermes/
/tools/
# local-hermes-agent/ is per-operator quality assessment / regression /
# inspection tooling (auditor cycle, smoke skill, scan-optimizer loop,
# launchd schedules, scheduler tokens, cycle logs, findings, audit reports).
# The whole tree is internal-only and NEVER shipped with the public
# agent product. Same rule applies to any future testing / optimization /
# auditing infrastructure.
/local-hermes-agent/
# All tests live under root tests/ per project policy and are gitignored
# (they're product quality assessment / regression / inspection — not part
# of the published agent runtime). The legacy colocation paths below are
# kept ignored so accidental re-creation doesn't leak back into git.
/tests/
/agent/tests/
/orchestrator/tests/
/orchestrator/backend/tests/
/orchestrator/frontend/src/test/
/orchestrator/backend/data/
/orchestrator/backend/*.egg-info/
/.superpowers/brainstorm/
/.opencode/vendor/
docs/**
!docs/
!docs/redteam-agent-demo-fast.gif
!docs/screenshot-20260429-045700.png
/tmp/
.venv/
/tmp-*/
/scratch/
/scratch-*/

# Track agent/.claude/ (runtime config) but ignore local overrides
!agent/.claude/

# ============================================
# Engagement artifacts (per-engagement, never commit)
# ============================================
agent/engagements/*/
!agent/engagements/.gitkeep
agent/engagements/.active

# ============================================
# Pentest tool output & artifacts
# ============================================

# nmap
*.nmap
*.gnmap
*.xml

# sqlmap
output/
.sqlmap/

# ffuf / gobuster / dirb
*.ffuf
*.gobuster

# Burp Suite
*.burp
*.jar

# Metasploit
*.rc
loot/
.msf4/

# Nuclei
nuclei-output/

# Nikto
*.csv

# Hydra / John / Hashcat
*.pot
*.restore
*.session

# General scan output
scan_results/
scan-results/
scans/

# ============================================
# Captured data & loot
# ============================================
loot/
dumps/
exfil/
*.dump
*.sql
!agent/scripts/schema.sql
*.bak
*.db
*.sqlite
*.sqlite3

# Credentials & wordlists (user-specific)
*.wordlist
creds/
credentials/

# Screenshots & evidence (large binaries)
screenshots/
*.png
*.jpg
*.jpeg
*.gif
*.bmp
!docs/redteam-agent-demo-fast.gif
!docs/screenshot-20260429-045700.png

# ============================================
# Generated scripts & payloads
# ============================================
payloads/
!agent/references/payloads/
exploits/
shells/
*.elf
*.exe
*.dll
*.so
*.war

# ============================================
# Node/TypeScript (plugin compilation)
# ============================================
node_modules/
orchestrator/frontend/dist/
*.js
*.d.ts
*.js.map
package-lock.json
yarn.lock

# ============================================
# OS & editor
# ============================================
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# ============================================
# Misc runtime
# ============================================
*.log
*.tmp
*.temp
*.pid
*.cache
__pycache__/
*.pyc
.env
.env.*
!.env.example
!agent/.env.example
!agent/.env.parallel.example

# ============================================
# CLI runtime files (local overrides, state, caches)
# ============================================

# Claude Code
agent/.claude/settings.local.json
agent/.claude/todos.json
agent/.claude/statsig/

# Codex
agent/.codex/config.toml
agent/.codex/state/

# Generated install-time build output
agent/build/
# Keep operator files (not generated by build)
!agent/.claude/agents/operator.md
!agent/.codex/agents/operator.toml

# ============================================
# agent/ is canonical; root-level duplicates of agent runtime are forbidden.
# Use agent/.opencode, agent/scripts, agent/skills, agent/references, agent/docker.
# To install the pre-commit guard: cp agent/scripts/hooks/block-root-dup-dirs.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit
# ============================================
/.opencode/
/scripts/
/skills/
/references/
/docker/

# Playwright MCP tool artifacts (snapshots, console logs, debug trees)
/.playwright-mcp/
